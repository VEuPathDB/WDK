{
  "name": "wdk",
  "version": "0.1.0",
  "devDependencies": {
    "clean-css": "^3.0.1",
    "jsx-loader": "^0.12.2",
    "traceur-loader": "^0.6.3",
    "webpack": "^1.4.14"
  },
  "scripts": {
    "build-js": "./scripts/concatLibs && ./node_modules/.bin/webpack -d",
    "build-css": "./scripts/buildCss",
    "clean": "rm -rf ./dist",
    "dist": "npm run clean && npm run build-js && npm run build-css"
  },
  "private": true,
  "dependencies": {
    "flux": "^2.0.1",
    "immutable": "^3.4.1",
    "react": "^0.12.2"
  }
}
