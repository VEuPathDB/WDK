/**
 * Classes to handle the streaming of data as a response.  One
 * advantage of using a web service over JSP is easier and more
 * transparent streaming of large data to the client.  These
 * classes can read data (from DB or file) and typically provide
 * a <code>javax.ws.rs.core.StreamingOutput</code> object. 
 */
package org.gusdb.wdk.service.stream;
