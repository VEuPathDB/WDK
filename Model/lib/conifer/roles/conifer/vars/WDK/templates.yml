# $SourceFileURL: WDK/trunk/Model/lib/conifer/roles/conifer/vars/WDK/templates.yml $
---
# src is relative to conifer/templates unless full path is given

model-config.xml:
  src: 'WDK/model-config.xml.j2'
  dest: '{{ gus_home }}/config/{{ project }}/model-config.xml'
model.prop:
  src: 'WDK/model.prop.j2'
  dest: '{{ gus_home }}/config/{{ project }}/model.prop'
tomcat_context.xml:
  src: 'WDK/tomcat_context.xml.j2'
  dest: '{{ gus_home }}/config/{{ webapp_ctx }}.xml'
